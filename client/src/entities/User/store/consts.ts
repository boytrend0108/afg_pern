export const REGISTER_PREFIX = {
  base: 'user/create',
  pending: 'user/create/pending',
  fulfilled: 'user/create/fulfilled',
  rejected: 'user/create/rejected',
};

export const LOGIN_PREFIX = {
  base: 'user/login',
  pending: 'user/login/pending',
  fulfilled: 'user/login/fulfilled',
  rejected: 'user/login/rejected',
};

export const LOGOUT_PREFIX = {
  base: 'user/logout',
  pending: 'user/logout/pending',
  fulfilled: 'user/logout/fulfilled',
  rejected: 'user/logout/rejected',
};

export const UPDATE_PREFIX = {
  base: 'user/update',
  pending: 'user/update/pending',
  fulfilled: 'user/update/fulfilled',
  rejected: 'user/update/rejected',
};

export const CHECK_AUTH_PREFIX = {
  base: 'user/auth',
  pending: 'user/auth/pending',
  fulfilled: 'user/auth/fulfilled',
  rejected: 'user/auth/rejected',
};
