// import { Button } from "react-bootstrap";

// export const AdminBrands = () => {
//   return (
//     <div className="AdminBrands">
//       <div className="AdminPage__buttons">
//         <Button onClick={() => setShowRole(true)}>Add role</Button>
//         <Button onClick={() => setShowBrand(true)}>Add brand</Button>
//         <Button onClick={() => setShowCategory(true)}>Add category</Button>
//         <Button onClick={() => setShowProduct(true)}>Add product</Button>
//       </div>

//       <Outlet />

//       <CreateRole show={showRole} onHide={() => setShowRole(false)} />
//       <CreateBrand show={showBrand} onHide={() => setShowBrand(false)} />
//       <CreateCategory
//         show={showCategory}
//         onHide={() => setShowCategory(false)}
//       />
//       <CreateProduct show={showProduct} onHide={() => setShowProduct(false)} />
//     </div>
//   );
// };
